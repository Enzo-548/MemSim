# Como Executar?
 - execute o comando:
```
python simulador_memoria.py <caminho do documento de entrada> <fifo/lru>
 ```
  - parametro fifo: Executará o código com o algoritmo **First In First Out.**
  - Parametro lru: Executará o código com o algoritmo **Least Recently Used**